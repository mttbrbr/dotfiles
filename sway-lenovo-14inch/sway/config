#### --- CORE SETTINGS ---
set $mod Mod4
set $term foot

### --- TAVOLOZZA COLORI GRAYISH SAGE ---
set $sa_bg       #111411
set $sa_bg_hard  #0d0f0d
set $sa_surface0 #1e241e
set $sa_surface1 #2a322a
set $sa_overlay  #3d4a3d
set $sa_fg_dim   #6b7a6b
set $sa_fg       #a8b8a0
set $sa_fg_bri   #c8d8be
set $sa_sage     #7a9e7e
set $sa_sage_dim #4d6e52
set $sa_warn     #b8a070
set $sa_err      #a06060

### --- ESTETICA ---
font pango:Iosevka Nerd Font 11
default_border pixel 1
default_floating_border pixel 1
smart_borders on
gaps inner 12
gaps outer 12

client.focused          $sa_sage     $sa_bg  $sa_fg_bri  $sa_sage     $sa_sage
client.focused_inactive $sa_surface1 $sa_bg  $sa_fg_dim  $sa_surface1 $sa_surface1
client.unfocused        $sa_bg_hard  $sa_bg  $sa_overlay $sa_bg_hard  $sa_bg_hard
client.urgent           $sa_err      $sa_bg  $sa_fg_bri  $sa_err      $sa_err

### --- OUTPUT ---
output * background $sa_bg_hard solid_color

### --- INPUT ---
input "type:touchpad" {
    natural_scroll enabled
    tap enabled
    pointer_accel 0.3
}
input "type:keyboard" {
    xkb_layout it
}
seat seat0 xcursor_theme Adwaita 24

### --- BARRA ---
bar {
    swaybar_command waybar
}

### --- TASTI DI SISTEMA ---
bindsym $mod+Return exec $term
bindsym $mod+Shift+q kill
bindsym $mod+Shift+c reload
bindsym $mod+Shift+e exec swaynag -t warning -m 'Esci?' -b 'Sì' 'swaymsg exit'

### --- NAVIGAZIONE FINESTRE ---
bindsym $mod+Left  focus left
bindsym $mod+Down  focus down
bindsym $mod+Up    focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+Left  move left
bindsym $mod+Shift+Down  move down
bindsym $mod+Shift+Up    move up
bindsym $mod+Shift+Right move right

### --- WORKSPACES ---
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

### --- LAYOUT ---
bindsym $mod+f           fullscreen toggle
bindsym $mod+v           split v
bindsym $mod+b           split h
bindsym $mod+s           layout stacking
bindsym $mod+w           layout tabbed
bindsym $mod+e           layout toggle split
bindsym $mod+Shift+space floating toggle
bindsym $mod+Shift+p     focus mode_toggle

### --- SCRATCHPAD ---
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus       scratchpad show

### --- LAUNCHER & MENU ---
bindsym $mod+d       exec fuzzel
bindsym $mod+Shift+f exec ~/.config/wofi/powermenu.sh

### --- LOCK ---
set $lock_cmd swaylock --color 0d0f0d --font 'Iosevka Nerd Font' --clock \
    --indicator-radius 60 --indicator-thickness 4 \
    --text-color a8b8a0 --ring-color 7a9e7e --key-hl-color c8d8be \
    --line-color 00000000 --inside-color 111411aa

bindsym $mod+l exec $lock_cmd

exec swayidle -w \
    timeout 300  '$lock_cmd' \
    timeout 600  'swaymsg "output * dpms off"' \
    resume       'swaymsg "output * dpms on"' \
    before-sleep '$lock_cmd'

### --- VOLUME ---
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym XF86AudioMute        exec pactl set-sink-mute @DEFAULT_SINK@ toggle

### --- LUMINOSITÀ ---
bindsym XF86MonBrightnessUp   exec brightnessctl set 5%+
bindsym XF86MonBrightnessDown exec brightnessctl set 5%-

bindsym $mod+space exec \
    brightnessctl -d "platform::kbd_backlight" get | grep -q 0 \
    && brightnessctl -d "platform::kbd_backlight" set 1 \
    || brightnessctl -d "platform::kbd_backlight" set 0

### --- SCREENSHOT ---
bindsym Print        exec grim ~/Pictures/$(date +%Y-%m-%d_%H-%M-%S).png
bindsym Shift+Print  exec grim -g "$(slurp)" ~/Pictures/$(date +%Y-%m-%d_%H-%M-%S).png
bindsym $mod+Shift+s exec grim -g "$(slurp)" - | wl-copy

### --- GESTURE ---
bindgesture swipe:right workspace prev
bindgesture swipe:left  workspace next
bindgesture swipe:up    exec brightnessctl set 5%+
bindgesture swipe:down  exec brightnessctl set 5%-

### --- WORKSPACE RENAME ---
bindsym $mod+Shift+m kill; exec swaymsg -t get_workspaces \
    | jq -r '.[] | .name' \
    | awk '{print "rename workspace " $1 " to " NR}' \
    | xargs -L1 swaymsg

### --- REGOLE FINESTRE ---
assign    [class="Firefox"] workspace number 2
for_window [class="Firefox"] fullscreen enable

### --- AUTOSTART ---
exec_always killall wlsunset 2>/dev/null; wlsunset -l 45.0 -L 9.0 -t 3500 -T 6500
exec_always gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'

exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway
exec systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec systemctl --user restart xdg-desktop-portal

bindsym $mod+r mode "resize"

# Setup per resizing delle finestre
mode "resize" {
    # Ridimensionamento con le frecce direzionali
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Ridimensionamento con i tasti in stile Vim (se li usi)
    bindsym h resize shrink width 10px
    bindsym j resize grow height 10px
    bindsym k resize shrink height 10px
    bindsym l resize grow width 10px

    # Premi Invio o Esc per tornare alla modalità normale
    bindsym Return mode "default"
    bindsym Escape mode "default"
}


# wallpapers
#output eDP-1 bg $HOME/Immagini/wallpapers/dan-otis-OYFHT4X5isg-unsplash.jpg fill
output * bg $HOME/Immagini/wallpapers/dan-otis-OYFHT4X5isg-unsplash.jpg fill

# setup schermi
output eDP-1 pos 0 0
# output HDMI-A-1 pos -2560 0
output HDMI-A-1 pos -320 -1440
#output HDMI-A-1 pos 1920 0

# Tema scuro
# Forza il tema scuro per le applicazioni GTK
   exec_always gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'
   exec_always gsettings set org.gnome.desktop.interface gtk-theme 'Adwaita-dark'

for_window [title="codex-pink-dot"] #floating enable, border none, resize set width 3 height 3, move position 900 -200, sticky enable
#floating_minimum_size 1 x 1
bindsym $mod+Shift+a exec ~/.local/bin/ask-selected-codex

# tema chiaro/scuro
bindsym $mod+Shift+t exec gsettings set org.gnome.desktop.interface color-scheme 'prefer-light'
